---
export interface Props {
  title: string;
  type: 'dataset' | 'tool' | 'resource' | 'app';
  description: string;
  image?: string;
  imageAlt?: string;
  tags?: string[];
  urls?: {
    github?: string;
    paper?: string;
    website?: string;
    viewer?: string;
  };
  status?: string;
  slug: string;
}

const { title, type, description, image, imageAlt, tags = [], urls = {}, status, slug } = Astro.props;
const basePath = import.meta.env.BASE_URL;
const baseUrl = basePath.endsWith('/') ? basePath : basePath + '/';

const typeColors: Record<typeof type, string> = {
  dataset: 'bg-blue-100 text-blue-800',
  tool: 'bg-green-100 text-green-800',
  resource: 'bg-purple-100 text-purple-800',
  app: 'bg-pink-100 text-pink-800'
};
---

<div class="card group flex flex-col h-full bg-white rounded-xl shadow-sm hover:shadow-md transition p-5 border border-gray-100">
  <div class="flex items-start justify-between mb-3">
    <div class="flex items-center space-x-2">
      <span class={`px-2 py-1 text-xs font-medium rounded-full ${typeColors[type] || 'bg-gray-100 text-gray-800'}`}>
        {type}
      </span>
      {status === 'archived' && <span class="px-2 py-1 text-xs font-medium rounded-full bg-red-100 text-red-800">archived</span>}
    </div>
  </div>
  
  {image && (
    <div class="aspect-w-16 aspect-h-9 mb-4 overflow-hidden rounded-lg bg-gray-100">
      <img 
        src={`${baseUrl}${image.replace(/^\//, '')}`} 
        alt={imageAlt || title}
        class="w-full h-32 object-cover group-hover:scale-105 transition-transform duration-300"
      />
    </div>
  )}
  
  <h3 class="text-lg font-semibold text-gray-900 mb-2 group-hover:text-tekhelet transition-colors duration-200">
    <a href={`${baseUrl}projects/${slug}/`}>{title}</a>
  </h3>
  
  <p class="text-gray-600 text-sm leading-relaxed mb-4 flex-grow">
    {description}
  </p>
  
  {tags && tags.length > 0 && (
    <div class="flex flex-wrap gap-2 mb-4">
      {tags.slice(0, 3).map(tag => (
        <span class="tag text-xs px-2 py-1 bg-gray-100 rounded text-gray-600">{tag}</span>
      ))}
      {tags.length > 3 && (
        <span class="tag text-xs px-2 py-1 bg-gray-100 rounded text-gray-600">+{tags.length - 3}</span>
      )}
    </div>
  )}
  
  <div class="flex gap-3 mt-auto pt-4 border-t border-gray-100">
    {urls.github && (
      <a href={urls.github} target="_blank" rel="noopener noreferrer" class="text-sm font-medium text-indigo-600 hover:text-indigo-800">
        GitHub
      </a>
    )}
    {urls.website && (
      <a href={urls.website} target="_blank" rel="noopener noreferrer" class="text-sm font-medium text-indigo-600 hover:text-indigo-800">
        Website
      </a>
    )}
    {urls.paper && (
      <a href={urls.paper} target="_blank" rel="noopener noreferrer" class="text-sm font-medium text-indigo-600 hover:text-indigo-800">
        {urls.paper}
      </a>
    )}
  </div>
</div>
